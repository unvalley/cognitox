[package]
name = "cognitox-demo-rust"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
aws-config = { version = "1", features = ["behavior-version-latest"] }
aws-sdk-cognitoidentityprovider = "1"
tokio = { version = "1", features = ["full"] }
